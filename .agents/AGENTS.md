# Custom Rules

## Code Change Process
* Do not make direct code changes to files or run modifying terminal commands.
* Instead, always present recommended code changes to the user (e.g., as code blocks, explanation, or diffs) and let the user implement the changes themselves.
